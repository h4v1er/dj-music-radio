# 🏗️ DJ 音乐电台 — 架构设计

> 本文档供答辩 PPT 引用：架构图、技术选型、数据库设计

---

## 一、总体架构

```
                    ┌──────────────────┐
                    │   VUE3 桌面控制台  │  :5173  (Vite)
                    │  Element Plus    │
                    └────────┬─────────┘
                             │ HTTP (Vite Proxy → Gateway)
                             ▼
                    ┌──────────────────┐
                    │   API Gateway     │  :8080  Spring Cloud Gateway
                    │   路由 + 限流     │
                    └────────┬─────────┘
                             │
              ┌──────────────┼──────────────┬──────────────┐
              ▼              ▼              ▼              ▼
        ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
        │ chat-svc│ │music-svc │ │ rec-svc  │ │ user-svc │
        │  :8081  │ │  :8082  │ │  :8083   │ │  :8084   │
        └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘
             │            │            │            │
             └────────────┴─────┬──────┴────────────┘
                                │
              ┌─────────────────┼─────────────────┐
              ▼                 ▼                 ▼
        ┌──────────┐    ┌──────────┐     ┌──────────────┐
        │  MySQL   │    │  Redis   │     │  RabbitMQ    │
        │  :3306   │    │  :6379   │     │  :5672       │
        └──────────┘    └──────────┘     └──────────────┘
              ▲
        ┌──────────┐
        │  Nacos   │  服务注册/发现 + 配置中心  :8848
        └──────────┘
```

---

## 二、技术选型

| 层级 | 技术 | 版本 | 选型理由 |
|:---|:---|:---|:-----|
| 框架 | Spring Boot | 3.4.5 | 课程要求，生态成熟 |
| ORM | MyBatis Plus | 3.5.9 | 课程要求，简化 CRUD |
| 微服务 | Spring Cloud | 2024.0.1 | Gateway + Nacos + OpenFeign |
| 注册/配置 | Nacos | 2.4.3 | 课程要求 |
| 缓存 | Redis | 8.8.0 | 热门榜单、验证码、Session |
| 消息队列 | RabbitMQ | 4.3.2 | 歌单异步导入、推荐推送 |
| 前端 | VUE3 + Vite | 3.x | 课程要求，现代化开发体验 |
| UI 库 | Element Plus | 最新 | 统一组件风格 |
| 数据库 | MySQL | 9.7.1 | 持久化存储 |
| JDK | OpenJDK | 25 LTS | Apple Silicon 原生支持 |
| 构建 | Maven | 3.9.11 | 多模块管理 |

---

## 三、数据库设计（核心表）

### 用户服务 (`module-user`)

```sql
CREATE TABLE user (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    nickname VARCHAR(50),
    avatar VARCHAR(255),
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE user_favorite (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    song_id INT NOT NULL,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 音乐服务 (`module-music`)

```sql
CREATE TABLE song (
    id INT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(255) NOT NULL,
    artist VARCHAR(100),
    album VARCHAR(100),
    cover_url VARCHAR(255),
    audio_url VARCHAR(255),
    duration INT COMMENT '秒',
    genre VARCHAR(50)
);

CREATE TABLE playlist (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    name VARCHAR(100) NOT NULL,
    description VARCHAR(500),
    is_public TINYINT DEFAULT 1
);

CREATE TABLE playlist_song (
    id INT PRIMARY KEY AUTO_INCREMENT,
    playlist_id INT NOT NULL,
    song_id INT NOT NULL,
    sort_order INT DEFAULT 0
);
```

### 推荐服务 (`module-rec`)

```sql
CREATE TABLE user_behavior (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    song_id INT NOT NULL,
    action ENUM('play','like','skip','share') NOT NULL,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE daily_recommend (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    song_id INT NOT NULL,
    reason VARCHAR(200),
    push_date DATE NOT NULL
);
```

### 对话服务 (`module-chat`)

```sql
CREATE TABLE chat_history (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    role ENUM('user','assistant') NOT NULL,
    content TEXT NOT NULL,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 四、微服务调用关系

```
module-chat ──OpenFeign──→ module-music   (DJ对话中搜索歌曲)
module-chat ──OpenFeign──→ module-rec     (DJ对话中获取推荐)
module-rec  ──OpenFeign──→ module-user    (推荐时获取用户偏好)
module-music──OpenFeign──→ module-rec     (播放时上报用户行为)
```

---

## 五、前端组件树

```
App.vue
├── WeatherWidget.vue      ← 队员A 天气
├── ChatPanel.vue          ← 队员A 对话
├── MusicPanel.vue          ← 队员B 播放器+歌单
├── RecPanel.vue            ← 队员C 推荐+榜单
└── UserBar.vue             ← 队员D 登录+收藏+历史
```

---

## 六、项目使用到的全部技术清单（答辩 PPT 用）

| 类别 | 技术 | 课程要求 | 本项目体现 |
|:---|:---|:---:|:---:|
| 核心框架 | Spring Boot | 必选 | 4 个微服务全部基于 Boot |
| MVC | Spring MVC | 必选 | Controller 层 REST API |
| ORM | MyBatis Plus | 必选 | 所有数据库操作 |
| 缓存 | Redis | 必选 | 热门榜单 ZSET + 验证码 + 行为采集 |
| 消息队列 | RabbitMQ | 必选 | 歌单导入 + 推荐推送 |
| 前端 | VUE3 | 必选 | 桌面式 DJ 控制台 |
| 注册中心 | Nacos | 必选 | 5 个服务全部注册 |
| 网关 | Gateway | 必选 | 统一路由 + 跨域 |
| 远程调用 | OpenFeign | 必选 | 4 条跨服务调用链路 |
| 构建工具 | Node.js | 必选 | Vite 前端开发服务器 |
| 搜索引擎 | Elasticsearch | 可选 | 音乐全文搜索（待集成） |
| 容器化 | Docker | 可选 | 打包部署（待集成） |
