# 开发时间线与当前进度

本文档从最初四人分工计划更新为当前真实集成进度。当前 `dev-user` 已合入并完成一次集成复核。

## 1. 当前基准

当前集成分支：

```text
dev-chat
```

当前已继续推进到：

```text
3bb1de3
feat(user): 整合用户中心登录与会话联动
```

`music`、`rec` 和 `user` 的阶段性最新代码已经合入当前分支。

## 2. 进度总览

| 阶段 | 状态 | 说明 |
|:--|:--|:--|
| 基础微服务骨架 | 已完成 | 父 POM、gateway、common、chat/music/rec/user 模块存在 |
| 前端单屏控制台 | 已完成阶段版 | `App.vue` 组合聊天、音乐、推荐、天气、时间、用户栏 |
| chat 模块 | 已完成阶段版 | AI 工具规划、WebSocket、历史、天气、时间、定位、music/rec 联动 |
| music 模块 | 已完成阶段版 | 网易云代理、播放器、歌单、导入、歌词、情绪、收藏历史 |
| rec 模块 | 已完成阶段版 | Redis 热榜、行为采集、每日推荐、相似推荐、偏好标签 |
| user 模块 | 已完成阶段版 | 注册、登录、JWT、用户资料、用户入口收藏/历史 |
| 新人运行文档 | 本轮补齐 | 新增 [新人运行手册.md](./新人运行手册.md) |
| 答辩说明文档 | 本轮补齐 | 新增 [项目完整说明书.md](./项目完整说明书.md) |

## 3. 已完成里程碑

### 里程碑 A：智能对话 DJ

- `POST /chat/send`；
- `/chat/ws` WebSocket；
- `chat_history` 持久化，数据库不可用时内存兜底；
- DeepSeek 工具规划和最终回复；
- 工具白名单：music、rec、weather、time、location；
- 和风天气真实 API；
- 浏览器定位工具；
- 聊天推荐歌曲可点击播放。

### 里程碑 B：音乐中心

- 本地歌曲列表、搜索、详情；
- 网易云搜索、播放 URL、详情、歌词、歌单、封面代理；
- 歌单 CRUD、排序、导入；
- RabbitMQ 异步导入；
- 歌词保存和情绪分析；
- 用户品味画像；
- 收藏和播放历史；
- 前端播放器和音乐子组件。

### 里程碑 C：推荐模块

- Redis ZSET 热榜；
- 用户行为上报和计分；
- 今日推荐；
- 相似歌曲推荐；
- 偏好标签；
- 每日 02:00 定时生成推荐；
- RabbitMQ 推荐通知。

### 里程碑 D：用户中心

- 注册和登录；
- BCrypt 密码哈希；
- JWT 签发和用户接口校验；
- 用户资料查询和更新；
- 收藏和播放历史入口；
- 收藏/历史统一委托 music 模块，避免两套数据；
- 前端登录态驱动 chat、music、rec 使用当前用户 ID。

### 里程碑 E：前端整合

- 单屏 DJ 控制台；
- 顶栏时间和天气；
- 左侧 AI 聊天；
- 中间播放器、歌曲、歌单、歌词、情绪；
- 右侧热门榜和推荐；
- 底部用户状态栏；
- 聊天推荐与播放器通过 `dj-play-song` 事件联动。

## 4. 仍未完成或需要复核

| 项目 | 当前问题 | 后续动作 |
|:--|:--|:--|
| Gateway 全局鉴权 | 目前只在 user 接口内校验 JWT | 后续可加 Gateway Filter 统一保护需要登录的接口 |
| 用户收藏和历史边界 | 已统一由 music 存储，user 作为带 token 的用户入口 | 后续继续保持单一数据源 |
| rec SQL | 已补独立 SQL 文件 | 新环境执行 `module-rec/src/main/resources/sql/rec_tables.sql` |
| Java 版本 | POM 是 25，默认 Java 可能是 17 | 全队统一 JDK25 或统一回退 |
| 前端代理 | `/music` 直连 8082 | 最终部署前决定是否全部走 Gateway |
| 数据初始化 | 数据库数据不随 Git 同步 | 运行手册说明 SQL 和导入方式 |

## 5. 下一阶段建议

1. 做一次完整端到端演示：启动环境 -> 登录 -> 聊天 -> 搜歌 -> 播放 -> 上报行为 -> 推荐刷新；
2. 继续打磨用户 token 过期、登录失败、空收藏/空历史等提示；
3. 评估是否补 Gateway 全局鉴权；
4. 等最后一次功能收口后，再按最终代码复核所有文档和答辩材料。
